# 운영 환경 Web 변수. 복사해서 `.env.production` 로 사용한다.
# NEXT_PUBLIC_* 는 빌드 시점에 박히므로 next build 전에 설정해야 한다.
# (docker 빌드 시에는 docker-compose 의 web.build.args 로 주입된다.)

NEXT_PUBLIC_API_BASE_URL=https://myvault.example.com
